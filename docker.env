# This file is used during the Docker build process
PORT=10000
NODE_ENV=production
LOG_LEVEL=info

# Dummy database config for build purposes only
# These will be overridden by the App Platform environment variables
DATABASE_URL=postgresql://builduser:buildpassword@localhost:5432/builddb